# setpoint-delivery-docket-extractor
Periodically polls a email inbox. When new email arrives with delivery docket the attachments is downloaded and sent to Azure Document Intelligence for OCR. OCR data is then saved to an excel spreadsheet in a sharepoint folder.
